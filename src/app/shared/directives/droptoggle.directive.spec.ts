import { DroptoggleDirective } from './droptoggle.directive';

describe('DroptoggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DroptoggleDirective();
    expect(directive).toBeTruthy();
  });
});
